{
  "name": "vitepress-theme-pm",
  "version": "0.2.0",
  "description": "A VitePress theme that adds a kanban board for project management with markdown tickets",
  "type": "module",
  "license": "MIT",
  "homepage": "https://github.com/McBrideMusings/vitepress-project-management",
  "repository": {
    "type": "git",
    "url": "https://github.com/McBrideMusings/vitepress-project-management.git"
  },
  "keywords": [
    "vitepress",
    "vitepress-theme",
    "kanban",
    "project-management",
    "markdown",
    "vue"
  ],
  "scripts": {
    "build": "esbuild src/plugins/markdownWriter.ts --format=esm --platform=node --bundle --external:gray-matter --external:vite --outfile=dist/plugin.mjs && npm run build:types",
    "build:types": "tsc -p tsconfig.plugin.json && mv dist/markdownWriter.d.ts dist/plugin.d.ts",
    "prepublishOnly": "npm run build"
  },
  "exports": {
    ".": "./src/index.ts",
    "./plugin": {
      "types": "./dist/plugin.d.ts",
      "default": "./dist/plugin.mjs"
    }
  },
  "bin": {
    "vitepress-theme-pm": "./src/cli.mjs"
  },
  "files": [
    "src",
    "dist"
  ],
  "peerDependencies": {
    "vitepress": ">=1.0.0",
    "vue": ">=3.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "esbuild": ">=0.17.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  }
}
